{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open":"cypress open",
    "test:dev":"cypress open --env ENV='DEV'",
    "test:prod":"cypress open --env ENV='PROD'"
    
  },
  "author": "Parag",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.1.0"
  },
  "devDependencies": {
    "cypress-iframe": "^1.0.1",
    "cypress-mochawesome-reporter": "^3.6.0",
    "cypress-xpath": "^2.0.1"
  }
}
